Fluent builder creates config, config passed to parser.
Parser takes config and builds a context for a single parsing operation.
Context begins parsing, stopping when:
  * Parsing completes
  * Error in parsing
  * Terminating trigger reached